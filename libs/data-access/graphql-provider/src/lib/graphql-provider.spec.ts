import GraphqlProvider from './graphql-provider';

describe('dataAccessGraphqlProvider', () => {
  it('should work', () => {
    expect(GraphqlProvider()).toEqual('data-access-graphql-provider');
  });
});
